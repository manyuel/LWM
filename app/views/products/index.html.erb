<div class="d-flex justify-content-center">
  <h1>All Products</h1>
</div>

<div class="container d-flex justify-content-between">
  <%# <div class="row"> %>
    <div class="container d-flex justify-content-between">
      <div class="col-lg-4">
        <div class="card-pic">
          <%= @products.each do |product| %>
              <%= render product, product: product %>
              <%# <div class="card-product-infos bg-light rounded-bottom py-2"> %>
                <div class="d-flex justify-content-around">
                  <p class="card-name text-decoration-none" link-light> <%= @user.name %> </p>
                    <p> ⭐ <%= @user.rating %></p>
                </div>
              <%# </div> %>
          <%end%>
        </div>
      </div>
    </div>
  <%# </div> %>
</div>

<%= link_to "Add a new product", new_product_path %>
