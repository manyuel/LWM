<div class="container3">
   <% if @listed_products.empty? %>
    <div>
      <p><%="You're yet to make any purchases!"%></p>
    </div>
  <% else %>
  <div>
    <h1>My Products</h1>
  </div>
  <div class="listed">
    <h2>Listed Products</h2>
</div>
<div class="btn">
    <%= link_to "Add a new product", new_product_path, class:"button" %>
</div>
  <div class="d-flex justify-content-start mt-3">
    <div class="basket">
      <% @listed_products.each do |product| %>
        <div class="card-product2 d-flex" >
          <%= cl_image_tag product.photos.first.key, width: 400, height: 350, crop: :fill %>
            <div class="flex-column">
                <ul style="list-style:none" class="ul">
                <li><%= product.item %></li>
                £<%= product.price.round(2) %>
                </ul>
                <%# <div style="width: 25%" class="d-flex flex-column justify-content-between text-bold"> %>
                  <%# <div class="d-flex justify-content-end pe-3"> %>
            </div>
            <div class="exit">
              <%= link_to 'X', products_path(product), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "text-decoration-none text-secondary mr-0" %>
            </div>
            <%# </div> %>
                <%# </div> %>

            <%# </div> %>
        </div>
      <%end%>
    </div>
  <% end %>
</div>

<% if @sold_products.empty? %>
  <div class="listed">
    <h2>Sold Products</h2>
  </div>
    <div>
      <p><%="You're yet to sell any items!"%></p>
    </div>
  <% else %>
  <div class="d-flex justify-content-between mt-3">
    <div class="basket">
      <% @sold_products.each do |product| %>
        <div class="card-product2 d-flex" >
          <%= cl_image_tag product.photos.first.key, width: 400, height: 350, crop: :fill %>
            <div class="flex-column">
              <ul style="list-style:none; width: 100%;" class="d-flex justify-content-between mt-1">
              <li><%= product.item %></li>
              £<%= product.price.round(2) %>
              </ul>
            </div>
            <div class="exit">
              <%= link_to 'X', products_path(product), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "text-decoration-none text-secondary" %>
            <%end%>
            </div>
        </div>
      <%end%>
    </div>
  </div>
</div>
