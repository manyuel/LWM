<div class="indexcard col-sm-12 col-lg-4">
  <%= link_to product, class: 'text-decoration-none text-dark' do %>
    <%= cl_image_tag product.photos.first.key, width: 350, height: 250, crop: :fill %>

    <div class="yo bg-white mb-4" style="height: 120px">
      <div class="w-full d-flex justify-content-around">
        <h3 class="item-namefont-normal flex-fill mt-2 px-2"><%= product.item %></h3>
      </div>
      <div class="noHover d-flex mb-2">
        <div class="d-flex align-items-center" >
          <%= cl_image_tag product.user.photo.key, style: "height: 50px; width: 50px;", class: "rounded" %>
          <p class="seller text-gray-500 text-sm p-1"style="margin-block-end: 0; margin-block-start: 0;"><%= product.user.name %></p>
        </div>
        <h5 class="fontw-bold ms-auto p-2">£<%= '%.2f' % product.price %></h5>
      </div>
    </div>
  <% end %>
</div>
